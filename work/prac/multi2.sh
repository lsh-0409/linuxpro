#!/bin/bash

# $1: 첫 번째 숫자 (10)
# $2: 연산자 (*)
# $3: 두 번째 숫자 (10)

# 방법 1: 이중 괄호(( ))를 사용한 산술 연산 (가장 권장됨)
result=$(($1 * $3))

# 방법 2: expr 명령어 사용 (구형 방식, 참고용)
# result=`expr $1 \* $3`

# 결과 출력
echo "곱셈: $1 $2 $3 = $result"/
